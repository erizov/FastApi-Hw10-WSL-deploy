# Быстрое руководство: Использование токена авторизации

## 1. Получение токена

### Через командную строку:
```bash
cd /mnt/e/Python/FastAPI/10
chmod +x deploy/get_token.sh
./deploy/get_token.sh
```

Или вручную:
```bash
curl -X POST "http://localhost:8000/auth/token/" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin"
```

### Через браузер (Swagger UI):
1. Откройте: **http://localhost:8000/docs**
2. Найдите эндпоинт `/auth/token`
3. Нажмите "Try it out"
4. Введите `username: admin` и `password: admin`
5. Нажмите "Execute"
6. Скопируйте `access_token` из ответа

## 2. Использование токена

### Вариант A: В браузере (Swagger UI)

1. Откройте: **http://localhost:8000/docs**
2. Нажмите кнопку **"Authorize"** (🔒) в правом верхнем углу
3. В поле **"Value"** вставьте токен (без слова "Bearer")
4. Нажмите **"Authorize"**, затем **"Close"**
5. Теперь все защищенные эндпоинты будут доступны

### Вариант B: В curl команде

```bash
# Сохраните токен в переменную
TOKEN="ваш_токен_здесь"

# Используйте токен в заголовке
curl -X GET "http://localhost:8000/order/" \
  -H "Authorization: Bearer $TOKEN"
```

### Вариант C: В Frontend приложении

```javascript
// После логина сохраните токен
localStorage.setItem('token', response.access_token);

// Используйте токен в запросах
const token = localStorage.getItem('token');
fetch('http://localhost:8000/order/', {
  headers: {
    'Authorization': `Bearer ${token}`
  }
})
```

### Вариант D: В Postman/Insomnia

1. Создайте запрос к защищенному эндпоинту
2. Перейдите на вкладку **"Authorization"**
3. Выберите тип: **"Bearer Token"**
4. Вставьте токен в поле **"Token"**

## 3. Примеры использования

### Получить список заказов:
```bash
curl -X GET "http://localhost:8000/order/" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Создать заказ:
```bash
curl -X POST "http://localhost:8000/order/" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"lead_id": 1, "status": "new"}'
```

### Получить список лидов:
```bash
curl -X GET "http://localhost:8000/lead/" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 4. Учетные данные по умолчанию

- **Username**: `admin`
- **Password**: `admin`

(Из файла `.env_example`)

## 5. Проверка токена

Токен действителен в течение времени, указанного в `AUTH_TOKEN_EXPIRE_MINUTES` (по умолчанию 30 минут).

Если получили ошибку `{"detail":"Not authenticated"}`:
- Проверьте, что токен не истек
- Убедитесь, что используете формат: `Authorization: Bearer TOKEN`
- Получите новый токен через `/auth/token`

